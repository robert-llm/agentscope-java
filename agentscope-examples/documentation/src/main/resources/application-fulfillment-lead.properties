# Fulfillment Lead Agent - Spring Boot Configuration
# Server port (can be overridden by SERVER_PORT environment variable)
server.port=${SERVER_PORT:18096}

# Team name (can be overridden by TEAM_NAME environment variable)
TEAM_NAME=${TEAM_NAME:OrderFulfillmentTeam}
